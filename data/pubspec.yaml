name: data
description: A starting point for Dart libraries or applications.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.0.5

dependencies:
  # path: ^1.8.0
  core:
    path: ../core

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
